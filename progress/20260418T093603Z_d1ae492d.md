# Progress — Feature Session d1ae492d

**Date:** 2026-04-18 09:36 UTC
**Session type:** feature
**Issue:** #3 — Split `Spec/ZstdTwoBlock.lean` into 3 files

## Accomplished

- Split `Zstd/Spec/TwoBlock.lean` (3493 lines) into three thematic submodules:
  - `Zstd/Spec/TwoBlockBase.lean` (959 lines): step theorems for raw/RLE blocks,
    `WellFormedSimpleBlocks` induction, homogeneous + heterogeneous raw/RLE
    structural compositions and block-level completeness, frame wrapping
    helper, `parseFrameHeader` lemmas, `windowSizeFromDescriptor` bounds.
  - `Zstd/Spec/TwoBlockCompressed.lean` (2024 lines): compressed literals
    step theorems, compressed sequences step, unified `WellFormedBlocks`
    predicate, and block-level composed completeness for every combination
    of compressed / compressed+compressed / compressed+raw-rle blocks.
  - `Zstd/Spec/TwoBlockComplete.lean` (528 lines): frame-level composed
    completeness for raw/RLE, `decompressFrame` position advancement,
    content characterization theorems, and the `frame_from_blocks` macro.
- Retained `Zstd/Spec/TwoBlock.lean` as a 23-line re-export so downstream
  `import Zstd.Spec.TwoBlock` (used by `Zstd/Spec/Succeeds.lean`) continues
  to build.
- Added new submodules to `Zstd.lean`.
- Dropped the dead `unfold_except` local macro declaration that was
  duplicated from `Zstd/Spec/Base.lean` but never invoked in `TwoBlock.lean`.

## Verification

- `lake build` succeeds (91/91 jobs).
- No new `sorry` introduced (pre-existing `sorry`s in `Zstd/Spec/XxHash.lean`
  are unrelated).
- All 58 theorem/definition signatures preserved (verified by sorted diff
  of signature lines between old and new files).
- Body content preserved verbatim (verified by md5 hash of non-blank
  lines in original lines 56–3491 vs. reassembled extractions).

## Notes / Decisions

- The issue's target of "under ~1500 lines per file" was met for
  `TwoBlockBase.lean` (959) and `TwoBlockComplete.lean` (528), but
  `TwoBlockCompressed.lean` is 2024 lines. Splitting further would
  deviate from the issue's explicit 3-file plan; all material in
  `TwoBlockCompressed.lean` naturally belongs together as "theorems
  involving at least one compressed block" per the issue description.
  Reviewers can decide whether a follow-up split (e.g. segregating the
  compressed/compressed completeness theorems into a fourth file) is
  desirable.
- The `frame_from_blocks` macro was moved (not duplicated) to
  `TwoBlockComplete.lean` since it is only used by frame-level content
  theorems which now live there.

## What remains

Nothing for this issue — all deliverables complete.

## Quality metrics

- `sorry` count in split files: 0 (unchanged)
- Line count of `Zstd/Spec/TwoBlock.lean`: 3493 → 23 (re-export)
- Total lines across the four resulting files: 3534 (vs. 3493 original,
  delta accounts for per-file headers and re-export)
