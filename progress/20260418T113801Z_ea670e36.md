# Progress: Rebase PR #17 onto main (issue #24)

**Date**: 2026-04-18 11:38 UTC
**Session**: ea670e36 (fix-PR session)
**Issue**: https://github.com/kim-em/lean-zstd/issues/24
**PR**: https://github.com/kim-em/lean-zstd/pull/17 (agent/d1ae492d)

## What was done

Rebased branch `agent/d1ae492d` (PR #17 — split `Spec/TwoBlock.lean` into
three thematic submodules) onto current `origin/main` and force-pushed
(`--force-with-lease`) so PR #17 picks up the resolution.

## Conflicts encountered and how they were resolved

The rebase surfaced two real conflicts, both caused by PR #15
(`refactor: eliminate runtime bounds bangs in parseFrameHeader`, commit
`507e1ab`) landing on main after the split branch diverged:

1. `Zstd.lean` — import list. Auto-merge placed the three new
   `TwoBlockBase`, `TwoBlockCompressed`, `TwoBlockComplete` imports next
   to the existing `Zstd.Spec.TwoBlock` re-export line, matching the
   natural thematic ordering requested by the issue. Accepted.
2. `Zstd/Spec/TwoBlock.lean` — entire file. On the split branch this
   file is reduced to a 23-line re-export stub; on main it is still the
   full ~3500-line original with PR #15's proof tweaks. Resolved by
   keeping the re-export stub (our side).

Since PR #15's modifications to the four `parseFrameHeader` proofs
(`parseFrameHeader_succeeds`, `parseFrameHeader_contentChecksum_eq`,
`parseFrameHeader_singleSegment_eq`, `parseFrameHeader_dictionaryId_eq`)
needed to survive the split, I manually ported the exact changes from
`507e1ab` into `Zstd/Spec/TwoBlockBase.lean`, which is where those four
theorems now live after the split:

- `parseFrameHeader_succeeds`: added `simp only [readByte_eq_getElem] at hres`
  after the Guard-3 rewrite, before the `generalize` block.
- `parseFrameHeader_contentChecksum_eq`: terminal `rfl` → `simp`.
- `parseFrameHeader_singleSegment_eq`: terminal `rfl` → `simp`.
- `parseFrameHeader_dictionaryId_eq`: added `simp only [readByte_eq_getElem] at *`
  before `grind`.

Verified by grep that the four theorem names appear only in
`TwoBlockBase.lean` (not in `TwoBlockCompressed.lean` or
`TwoBlockComplete.lean`).

## Verification

- `lake build` — 139/139 jobs, `Build completed successfully`.
- `lake exe test` — `All tests passed!`.
- `grep sorry` — only 3 pre-existing in `Zstd/Spec/XxHash.lean`
  (unchanged; no new sorry introduced).
- `gh pr view 17 --json mergeable --jq .mergeable` → `MERGEABLE`.

## Notes

- No new PR created per issue guidance — the original PR #17 review
  continuity is preserved via force-push with lease.
- Force-push used `--force-with-lease` after `git fetch origin
  agent/d1ae492d` per project policy.
- Issue #24 will be closed manually (PR #17's body closes the original
  #3, not #24).
