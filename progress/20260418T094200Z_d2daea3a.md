# Progress — 2026-04-18 09:42 UTC

**Session**: d2daea3a (feature)
**Issue**: #8 — Split `Spec/Frame.lean` — extract completeness files

## Accomplished

Split `Zstd/Spec/Frame.lean` (2322 lines) along the simple-first vs
compressed-first boundary already described in its own doc header:

- New `Zstd/Spec/FrameSimpleComplete.lean` (813 lines): single and
  two-block composed completeness where the first block is raw or RLE,
  including raw/RLE → compressed-sequences mixed layouts.
- New `Zstd/Spec/FrameCompComplete.lean` (1146 lines): single and
  two-block composed completeness where the first block is compressed.
- Trimmed `Zstd/Spec/Frame.lean` (400 lines): frame metadata theorems
  (`decompressZstd_single_frame_contentSize`, `_checksum`) plus the
  unified `WellFormedSimpleBlocks` / `WellFormedBlocks` completeness
  layer and its N-block demonstration corollaries.

`Zstd.lean` picks up both new submodules. `Zstd/Spec/Frame.lean` now
imports the two new files so existing `import Zstd.Spec.Frame` sites
continue to work transparently.

## Decisions / Patterns

- The file's doc header already advertised the two themes ("raw/RLE-first"
  vs "compressed-first"); split by first-block type, not by section
  heading text. The old heading at line 1411 said "comp_sequences first,
  raw/RLE second" but the section actually mixed two raw/rle-first
  theorems in at the end — those moved to the simple file based on their
  theorem name, not their old section neighbourhood.
- Verified no cross-references between the two new files: no
  simple-first theorem references any `compressed_*_then_*` theorem
  and no compressed-first theorem references `raw_then_*`, `rle_then_*`,
  or single-raw/rle theorems. Both new files depend only on `FrameBase`.
- Frame metadata theorems (content size, checksum) stayed with the unified
  layer since the issue calls them out as cross-cutting metadata.

## Verification

- `lake build`: passes (89/89).
- Sorry count in `Zstd/Spec/` unchanged at 4 (all pre-existing in
  `Zstd/Spec/XxHash.lean`).
- All theorem signatures preserved verbatim — this is a pure move.
- Frame.lean is 400 lines, well under the 1000-line target.

## Remains

Nothing for this issue. Sibling splits (#12 Fse, #14 Sequence, and the
blocked #13 Huffman) remain for other workers.
